let Temp = 0
let Anzahl = 0
while (Anzahl < 5) {
    Anzahl += 1
    basic.showNumber(Anzahl)
}
basic.forever(function () {
    Temp = input.temperature()
    if (Temp < 0) {
        if (Temp < 0) {
            basic.showNumber(Temp)
        }
    }
})
